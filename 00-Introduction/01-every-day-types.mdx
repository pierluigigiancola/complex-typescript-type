Abbiamo i tipi primitivi come `string`, `number`, `boolean`, `null`, `undefined` e `symbol`.

Abbiamo anche i tipi di oggetti come `object`, `array`, `function`.

Per un overview completa rimando alla documentazione: [Typescript Handbook](https://www.typescriptlang.org/docs/handbook/2/everyday-types.html)
